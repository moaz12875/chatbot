services:
  - type: web
    name: chatbot-flask
    env: python
    region: frankfurt  # أو أي region تحبه
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
